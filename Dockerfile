# Используйте официальный образ Node.js как базовый
FROM node:14-alpine

# Установите рабочую директорию в контейнере
WORKDIR /app

# Копируйте файлы package.json и package-lock.json
COPY package*.json ./

# Установите зависимости проекта
RUN npm install

# Копируйте исходный код проекта в рабочий каталог
COPY . .

# Откройте порт, который слушает ваше приложение
EXPOSE 8080

# Запуск приложения при старте контейнера будет определяться в docker-compose
CMD ["npm", "run", "start"]